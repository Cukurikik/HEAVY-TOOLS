import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class WatermarkService {
  prepareConfig() {
     // Specific algorithms for Watermark mapping options
     return {};
  }
}
