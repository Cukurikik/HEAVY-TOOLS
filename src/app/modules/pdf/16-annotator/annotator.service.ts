import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class AnnotatorService {
  prepareConfig() {
     // Specific algorithms for Annotator mapping options
     return {};
  }
}
