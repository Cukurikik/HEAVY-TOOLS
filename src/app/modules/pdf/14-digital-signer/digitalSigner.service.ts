import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class DigitalSignerService {
  prepareConfig() {
     // Specific algorithms for DigitalSigner mapping options
     return {};
  }
}
