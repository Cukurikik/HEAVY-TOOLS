import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class CompareService {
  prepareConfig() {
     // Specific algorithms for Compare mapping options
     return {};
  }
}
