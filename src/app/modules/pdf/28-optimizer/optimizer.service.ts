import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class OptimizerService {
  prepareConfig() {
     // Specific algorithms for Optimizer mapping options
     return {};
  }
}
