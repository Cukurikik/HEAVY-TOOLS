import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class FormFillerService {
  prepareConfig() {
     // Specific algorithms for FormFiller mapping options
     return {};
  }
}
