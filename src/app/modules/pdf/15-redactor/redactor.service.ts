import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class RedactorService {
  prepareConfig() {
     // Specific algorithms for Redactor mapping options
     return {};
  }
}
