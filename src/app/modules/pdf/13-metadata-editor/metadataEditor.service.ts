import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class MetadataEditorService {
  prepareConfig() {
     // Specific algorithms for MetadataEditor mapping options
     return {};
  }
}
