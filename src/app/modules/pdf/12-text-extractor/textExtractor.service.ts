import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class TextExtractorService {
  prepareConfig() {
     // Specific algorithms for TextExtractor mapping options
     return {};
  }
}
