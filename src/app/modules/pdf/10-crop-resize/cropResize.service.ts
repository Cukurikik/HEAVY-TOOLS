import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class CropResizeService {
  prepareConfig() {
     // Specific algorithms for CropResize mapping options
     return {};
  }
}
