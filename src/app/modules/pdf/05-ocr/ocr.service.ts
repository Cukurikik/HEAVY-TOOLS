import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class OcrService {
  prepareConfig() {
     // Specific algorithms for Ocr mapping options
     return {};
  }
}
