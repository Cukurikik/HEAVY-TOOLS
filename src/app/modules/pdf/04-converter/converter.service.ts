import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ConverterService {
  prepareConfig() {
     // Specific algorithms for Converter mapping options
     return {};
  }
}
