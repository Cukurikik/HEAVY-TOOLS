import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class PasswordProtectorService {
  prepareConfig() {
     // Specific algorithms for PasswordProtector mapping options
     return {};
  }
}
