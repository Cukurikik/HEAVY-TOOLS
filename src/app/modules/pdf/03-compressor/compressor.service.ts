import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class CompressorService {
  prepareConfig() {
     // Specific algorithms for Compressor mapping options
     return {};
  }
}
