import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class RotatorService {
  prepareConfig() {
     // Specific algorithms for Rotator mapping options
     return {};
  }
}
