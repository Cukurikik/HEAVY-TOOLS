import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class FlattenerService {
  prepareConfig() {
     // Specific algorithms for Flattener mapping options
     return {};
  }
}
