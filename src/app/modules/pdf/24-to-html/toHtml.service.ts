import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ToHtmlService {
  prepareConfig() {
     // Specific algorithms for ToHtml mapping options
     return {};
  }
}
