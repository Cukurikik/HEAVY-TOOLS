import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class MergerService {
  prepareConfig() {
     // Specific algorithms for Merger mapping options
     return {};
  }
}
