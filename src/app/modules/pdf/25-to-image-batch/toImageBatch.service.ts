import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ToImageBatchService {
  prepareConfig() {
     // Specific algorithms for ToImageBatch mapping options
     return {};
  }
}
