import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class SplitterService {
  prepareConfig() {
     // Specific algorithms for Splitter mapping options
     return {};
  }
}
