import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class RepairService {
  prepareConfig() {
     // Specific algorithms for Repair mapping options
     return {};
  }
}
