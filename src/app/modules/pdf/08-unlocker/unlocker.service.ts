import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class UnlockerService {
  prepareConfig() {
     // Specific algorithms for Unlocker mapping options
     return {};
  }
}
