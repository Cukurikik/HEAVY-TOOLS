import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ToWordService {
  prepareConfig() {
     // Specific algorithms for ToWord mapping options
     return {};
  }
}
