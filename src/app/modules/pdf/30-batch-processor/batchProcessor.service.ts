import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class BatchProcessorService {
  prepareConfig() {
     // Specific algorithms for BatchProcessor mapping options
     return {};
  }
}
