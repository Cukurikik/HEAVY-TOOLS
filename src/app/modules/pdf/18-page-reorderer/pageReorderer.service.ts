import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class PageReordererService {
  prepareConfig() {
     // Specific algorithms for PageReorderer mapping options
     return {};
  }
}
