import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class BookmarkEditorService {
  prepareConfig() {
     // Specific algorithms for BookmarkEditor mapping options
     return {};
  }
}
